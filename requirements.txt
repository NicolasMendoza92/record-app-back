# Transcripción en la nube (STT) — proveedor por defecto: AssemblyAI
assemblyai

# API y servidor
fastapi
uvicorn[standard]
python-multipart

# Claude (generación del acta)
anthropic

# Utilidades
python-dotenv
rich
